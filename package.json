{
  "name": "load-tests",
  "version": "0.1.0",
  "private": true,
  "description": "k6 scenarios for exercising load-test API endpoints.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "k6:smoke": "npm run build && k6 run dist/smoke/bootstrap.js"
  },
  "devDependencies": {
    "@types/k6": "^0.45.2",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
